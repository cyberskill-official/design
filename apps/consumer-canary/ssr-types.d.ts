import type { ReactElement, ReactNode } from "react";
import type { ThemeName, ContrastName } from "../../components/_theme/provider.d.ts";

export declare function renderCanary(children?: ReactNode): ReactElement;
export declare const theme: ThemeName;
export declare const contrast: ContrastName;
