**DatePicker** — field + popover Calendar; VN format DD/MM/YYYY, Monday week-start. The popover is `role="dialog"` and takes its accessible name from `label` (fallback: placeholder).

```jsx
<DatePicker value={date} onChange={setDate}/>
```
