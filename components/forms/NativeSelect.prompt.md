**NativeSelect** — progressive-enhancement native `<select>` styled to CS tokens beside Select. Supports `size`, label, and inline error.

```jsx
<NativeSelect
  label="Squad"
  size="sm"
  options={[{value:"plat",label:"Platform"},{value:"ai",label:"AI"}]}
  defaultValue="ai"
/>
```
