**ScrollArea** — focusable overflow region for dense panels (settings, long bilingual copy). Pass `maxHeight` to cap the scrollport.

```jsx
<ScrollArea maxHeight={240} aria-label="Notification preferences">
  {/* long list */}
</ScrollArea>
```
