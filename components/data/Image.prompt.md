**Image** — img with shimmer, warm error fallback, optional `preview` lightbox. `width` / `height` forward to the underlying `<img>`.

```jsx
<Image src="/assets/aurora-gold.jpg" alt="Aurora" ratio="16/9" width={640} height={360} preview/>
```
