window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-002"]="<h1>TASK-IMP-002: Mentions APG editable-combobox keyboard</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-012: the Mentions suggestion listbox is keyboard-inoperable at tip (<code>onMouseDown</code> only).</p>\n<ul><li>1.1 Implement the APG editable-combobox pattern over the existing listbox: track <code>activeIndex</code>; set <code>aria-activedescendant</code> on the textarea; stable option <code>id</code>s.</li><li>1.2 ArrowUp/Down navigate; Enter → existing <code>pick()</code>; Escape closes; mouse path remains (<code>onClick</code>/<code>onMouseDown</code> with focus retention).</li><li>1.3 Propagate <code>.d.ts</code> / <code>.prompt.md</code> / Atomic View / behavior harness per Expansion Rule.</li><li>1.4 Hard keyboard fixture MAY land with TASK-IMP-006; a stub fixture is acceptable here if 006 follows immediately.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1–1.2 — keyboard-only open → arrow → select a mention — demo + fixture (or stub linked to 006) — behavior: <code>Mentions — keyboard arrow + Enter picks</code> / Escape; Playwright keyboard fixture deferred to TASK-IMP-006</li><li>[x] AC for 1.3 — specimen/docs/Atomic View/behavior coverage updated — Expansion Rule checklist</li><li>[x] AC for 1.4 — no regression of mouse pick path — behavior harness (<code>Mentions — type @ filters suggestions</code> still green)</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Empty hits / closed popup: no activedescendant; arrows no-op or reopen per APG choice documented in § notes.</li><li>IME composition: do not steal keys during composition.</li></ul>\n<h2>4. Protected invariants this task must not weaken</h2>\n<ul><li>Do not remove the listbox role or break existing <code>pick()</code> consumers.</li><li>Sortable / publish / gates out of scope.</li></ul>\n<p><em>End of TASK-IMP-002.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"i accept all, continue\"</strong> — Phases 0–2 tranche (TASK-IMP-001…008) accepted. Status set to <code>done</code>.</p>";
