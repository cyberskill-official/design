window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-012"]="<h1>TASK-IMP-012: Recommend dist/styles.min.css as static default</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-017: documented static default is the 25-file <code>@import</code> waterfall (<code>styles.css</code>) while flattened <code>dist/styles.min.css</code> already exists.</p>\n<ul><li>1.1 <code>docs/consuming.md</code> static-adoption step SHALL recommend <code>dist/styles.min.css</code> (or <code>@cyberskill/design/styles.min.css</code>) for production; keep <code>styles.css</code> as readable source/dev.</li><li>1.2 <code>examples/static-hello/index.html</code> (+ README) SHALL link the flattened stylesheet by default.</li><li>1.3 VI consuming docs updated if they mirror the static path.</li><li>1.4 This task MUST NOT be marked <code>done</code> without HITL.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — consuming.md static path recommends flattened CSS — inspect</li><li>[x] AC for 1.2 — static-hello links <code>dist/styles.min.css</code> — inspect</li><li>[x] AC for 1.3 — VI parity if file exists</li><li>[x] AC for 1.4 — human review + final acceptance — HITL only (operator 2026-08-09)</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Bundler npm path may still import <code>styles.css</code> or <code>styles.min.css</code>; do not break <code>exports[\"./styles.css\"]</code>.</li><li>Kitchen-sink / templates that already use flattened via <code>ds-base.js</code> need no change.</li></ul>\n<h2>4. Protected invariants this task must not weaken</h2>\n<ul><li>Token/<code>styles.css</code> source tree remains the authoring source of truth.</li></ul>\n<p><em>End of TASK-IMP-012.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"continue all of them\"</strong> — Phase 3 tranche (TASK-IMP-009…012) accepted. Status set to <code>done</code>.</p>";
