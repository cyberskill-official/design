window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-016"]="<h1>TASK-IMP-016: Tighten CSP + portable security headers</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-015 and FIND-019.</p>\n<ul><li>1.1 Tighten <code>vercel.json</code> CSP: drop <code>unsafe-eval</code> where the built site still works; remove or pin unnecessary CDN script/connect origins now that React is vendored locally; keep only required <code>unsafe-inline</code> with a documented rationale if Storybook still needs it.</li><li>1.2 Reconcile divergent CSP texts in <code>docs/deploy.md</code> (EN+VI) with the live <code>vercel.json</code> policy.</li><li>1.3 Port the same header set (CSP, X-Content-Type-Options, Referrer-Policy) to every documented host path (nginx snippet and/or <code>public/_headers</code> / Netlify-equivalent as documented).</li><li>1.4 Leave HITL for final acceptance — land at <code>ready_to_review</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — CSP tightened; no silent CDN free-for-all — vercel.json</li><li>[x] AC for 1.2 — deploy docs match vercel.json — docs/deploy.md (+ VI)</li><li>[x] AC for 1.3 — every documented host has the header set — deploy.md / _headers</li><li>[x] AC for 1.4 — human review + final acceptance — HITL only (operator 2026-08-09)</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>If Storybook still requires <code>unsafe-eval</code>, document a route-scoped exception rather than silently keeping a site-wide free pass without rationale.</li><li>OQ-003 (live served header) stays NV unless measurable in-repo.</li></ul>\n<h2>4. Protected invariants</h2>\n<ul><li><code>_audit/</code> remains intentionally public.</li><li>Do not invent live-site verification claims without evidence.</li></ul>\n<h2>5. Implementation notes (Aug 2026)</h2>\n<p>Dropped unsafe-eval + CDN origins; kept unsafe-inline (Storybook); mirrored CSP in <code>_headers</code> + nginx deploy docs EN/VI.</p>\n<p><em>End of TASK-IMP-016.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"accept all, continue\"</strong> — Phase 4–5 tranche (TASK-IMP-013…020) accepted. Status set to <code>done</code>.</p>";
