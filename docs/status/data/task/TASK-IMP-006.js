window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-006"]="<h1>TASK-IMP-006: Extend docs-consistency + keyboard fixtures + asset budget</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-018 — highest-leverage Phase-2 gate extension.</p>\n<ul><li>1.1 <strong>Doc/token parity</strong> in <code>_audit/docs-consistency.html</code>: assert README/SKILL role-token count (incl. <code>on-strong</code>), depth-scale tiers, documented default lang vs <code>components/_i18n/i18n.js</code>, breakpoint token usage where in scope (closes FIND-003/005/009 recurrence).</li><li>1.2 <strong>Playwright keyboard fixtures</strong> for Mentions + Sortable (<strong>hard-fail</strong>). Stubs/structure for Rating/Tree/Toolbar may exist but Phase-3 widgets MUST NOT hard-fail this tranche unless those remediations land.</li><li>1.3 <strong>Asset-weight budget</strong> gate for aurora PNGs (fail on regression / oversized set; conversion itself may stay Phase 4).</li><li>1.4 Wire new/changed gates into <code>_audit/index.html</code> / Status board per Expansion Rule / CLAUDE.md.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — docs-consistency fails on role/depth/lang drift — gate run (<code>docs-consistency.html</code> + Node twin <code>test-docs-consistency.mjs</code>)</li><li>[x] AC for 1.2 — Mentions + Sortable keyboard fixtures hard-fail on regression — <code>a11y-gate.html</code> (+ behavior harness already covered)</li><li>[x] AC for 1.3 — asset budget gate green on tip; fails if aurora set grows past budget — <code>asset-weight-budget.html</code></li><li>[x] AC for 1.4 — <code>_audit/index.html</code> + Status surfaces list the new gates (<code>run.html</code> board = 40)</li></ul>\n<h2>Implementation notes (2026-08-09)</h2>\n<p>Landed with TASK-IMP-007 doc/z-index fixes so new assertions stay green. Rating/Tree/Toolbar APG intentionally not hard-failed (Phase 3).</p>\n<h2>3. Edge cases</h2>\n<ul><li>Rating/Tree/Toolbar: TODO/skip only if product policy forbids failing unfinished widgets; prefer structure without hard-fail until Phase 3.</li><li>Do not weaken existing fast gates while extending.</li></ul>\n<h2>4. Protected invariants this task must not weaken</h2>\n<ul><li>Expansion Rule: new gates appear on browsable + health surfaces.</li><li>Deep verification: whole-set assertions, not spot-checks.</li></ul>\n<p><em>End of TASK-IMP-006.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"i accept all, continue\"</strong> — Phases 0–2 tranche (TASK-IMP-001…008) accepted. Status set to <code>done</code>.</p>";
