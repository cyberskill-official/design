window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-015"]="<h1>TASK-IMP-015: Language-independent formatCurrency</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-025.</p>\n<ul><li>1.1 <code>formatCurrency(amount, { currency, locale } | lang)</code> SHALL use <code>Intl.NumberFormat</code> with <code>style: 'currency'</code>; currency MUST NOT be forced by language alone when an explicit currency is provided.</li><li>1.2 Backward-compatible defaults: when called as <code>formatCurrency(n, lang)</code> with a string lang, preserve prior EN→USD / VI→VND presentation.</li><li>1.3 Finance templates (<code>finance-invoice</code>, <code>finance-quote</code>, <code>finance-expense-report</code>, <code>finance-dunning-email</code>) SHALL state transaction currency independently of Language (label/code or helper), so EN+VND and VI+USD are expressible.</li><li>1.4 Leave HITL for final acceptance — land at <code>ready_to_review</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — VND in EN / USD in VI via options — unit test</li><li>[x] AC for 1.2 — string-lang call shape unchanged in output — unit test</li><li>[x] AC for 1.3 — finance templates declare currency ≠ language — inspect templates</li><li>[x] AC for 1.4 — human review + final acceptance — HITL only (operator 2026-08-09)</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Null/NaN amounts still return <code>\"\"</code>.</li><li>Bundle regeneration must pick up i18n.js changes.</li></ul>\n<h2>4. Protected invariants</h2>\n<ul><li>Bilingual EN·VI finance draft banners unchanged.</li></ul>\n<h2>5. Implementation notes (Aug 2026)</h2>\n<p><code>formatCurrency(amount, {currency, locale})</code> via Intl; legacy string-lang unchanged; finance templates declare VND transaction currency; <code>test-format-currency</code>.</p>\n<p><em>End of TASK-IMP-015.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"accept all, continue\"</strong> — Phase 4–5 tranche (TASK-IMP-013…020) accepted. Status set to <code>done</code>.</p>";
