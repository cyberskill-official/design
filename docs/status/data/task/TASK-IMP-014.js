window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-014"]="<h1>TASK-IMP-014: Tokenize theme-following base CSS hex</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-010 rule-by-rule.</p>\n<ul><li>1.1 Replace theme-/element-following raw hex in <code>base/*.css</code> with matching <code>--cs-color-*</code> (or role) tokens.</li><li>1.2 Keep an allowlist for genuine <code>color-mix</code> anchors and documented focus-ring fallbacks.</li><li>1.3 Add a Node gate (or docs-consistency extension) that fails on non-allowlisted bare hex in <code>base/</code>.</li><li>1.4 Leave HITL for final acceptance — land at <code>ready_to_review</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — theme-following literals tokenized — inspect base CSS</li><li>[x] AC for 1.2 — allowlist documented for color-mix / ring fallbacks — gate allowlist + comment</li><li>[x] AC for 1.3 — gate enforces allowlist — unit test green</li><li>[x] AC for 1.4 — human review + final acceptance — HITL only (operator 2026-08-09)</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Dark-only system-theme twins may keep literals only when no token exists; prefer token overrides.</li><li>Do not break glass liquid-glass mixes that require fixed anchors.</li></ul>\n<h2>4. Protected invariants</h2>\n<ul><li>Mid-tone <code>-accent</code> text-on-fill ban unchanged.</li><li>Contrast-guard / APCA floors stay green.</li></ul>\n<h2>5. Implementation notes (Aug 2026)</h2>\n<p>Tokenized theme-following surfaces (panel, info-fg); code/terminal chrome → local custom props; on-brand <code>#fff</code> allowlisted; <code>test-base-hex-allowlist</code> gate.</p>\n<p><em>End of TASK-IMP-014.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"accept all, continue\"</strong> — Phase 4–5 tranche (TASK-IMP-013…020) accepted. Status set to <code>done</code>.</p>";
