window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-013"]="<h1>TASK-IMP-013: Converge responsive breakpoints on token scale</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-007.</p>\n<ul><li>1.1 Every <code>max-width</code> / <code>min-width</code> pixel stop in <code>base/**/*.css</code> (and <code>@container</code> max-width used as a breakpoint) SHALL be drawn from the sanctioned breakpoint set in <code>tokens/spacing.css</code> / <code>tokens.json</code>, plus any explicitly documented extension tokens (e.g. phone <code>360px</code> for the 320 reflow band).</li><li>1.2 Collapse near-duplicate ad-hoc stops (600/620 → one token stop; 760→md; 900→md or lg; 1120→xl) so responsive behavior matches <code>guidelines/spacing-breakpoints.html</code>.</li><li>1.3 Extend <code>docs-consistency</code> (Node + browser) to fail on any off-scale <code>@media</code>/<code>@container</code> width in <code>base/</code>.</li><li>1.4 Leave HITL for final acceptance — land at <code>ready_to_review</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — media/container widths ⊆ sanctioned set — inspect <code>base/**</code> + tokens</li><li>[x] AC for 1.2 — near-duplicates gone; guideline matches — <code>spacing-breakpoints.html</code> + responsive.css</li><li>[x] AC for 1.3 — gate fails on invented widths — <code>test:docs-consistency</code> / docs-consistency.html</li><li>[x] AC for 1.4 — human review + final acceptance — HITL only (operator 2026-08-09)</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Preference queries (<code>prefers-*</code>, <code>pointer</code>, <code>print</code>, <code>forced-colors</code>) are not width stops — leave untouched.</li><li>Justified phone stop (360) must be a named token if retained.</li></ul>\n<h2>4. Protected invariants</h2>\n<ul><li>Do not weaken 320px reflow (UX-003) coverage.</li><li>Expansion Rule: update guideline + docs when the scale gains a tier.</li></ul>\n<h2>5. Implementation notes (Aug 2026)</h2>\n<p>Converged base media/container widths onto token scale; added <code>--cs-breakpoint-phone: 360px</code>; docs-consistency Node+browser assert sanctioned widths.</p>\n<p><em>End of TASK-IMP-013.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"accept all, continue\"</strong> — Phase 4–5 tranche (TASK-IMP-013…020) accepted. Status set to <code>done</code>.</p>";
