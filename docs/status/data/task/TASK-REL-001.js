window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-REL-001"]="<h1>TASK-REL-001: LAUNCH design system at VERSION 1.1.0</h1>\n<h2>1. Description (normative)</h2>\n<p>Owner said <strong>LAUNCH</strong> and set the release to <strong>1.1.0</strong>. The pre-LAUNCH pin at 1.0.0 is lifted for this cut only; further bumps still need an explicit owner instruction. No <code>CHANGELOG.md</code> — continuity stays tip SHA + curated Release Notes.</p>\n<ul><li>1.1 Root <code>VERSION</code> and every version stamp (<code>package.json</code>, token metas, DTCG <code>$extensions</code>, <code>provenance.json</code>, <code>DESIGN.md</code> front matter, <code>_esm/cs.mjs</code> <code>VERSION</code>, embedded token meta in <code>_ds_bundle.js</code>) SHALL equal <strong>1.1.0</strong>.</li><li>1.2 Hard gates that previously asserted the <strong>1.0.0</strong> pin (<code>docs-consistency</code>, <code>version-stamp</code>, <code>test-react-entry</code>, <code>npm-publish.mjs</code>) SHALL assert <strong>1.1.0</strong> (or equality with the root <code>VERSION</code> file) and SHALL NOT retain \"until LAUNCH\" blocking language that forbids this bump.</li><li>1.3 Operator-facing docs (EN + VI), SKILL/README/CONTRIBUTING, Release Notes, and Storybook Release Notes SHALL record LAUNCH at <strong>1.1.0</strong> and that further VERSION bumps require owner instruction; still no changelog file.</li><li>1.4 <code>examples/npm-hello</code> SHALL target <code>@cyberskill/design@1.1.0</code>. Until that version exists on the public registry, CI MAY soft-skip the registry install with an honest report and prove the local pack instead — soft-skip ≠ published.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[ ] AC for 1.1 — <code>VERSION</code> and all stamps read <code>1.1.0</code> — test: <code>version-stamp</code> / <code>scripts/verify-local.sh</code></li><li>[ ] AC for 1.2 — docs-consistency + react-entry + npm-publish dry-run accept 1.1.0 — test: <code>_audit/docs-consistency.html</code>, <code>npm run test:react-entry</code>, <code>npm run npm:pack-dry-run</code></li><li>[ ] AC for 1.3 — grep for \"until LAUNCH\" / \"pinned at <strong>1.0.0</strong>\" in live operator docs is clean (archive may retain history) — test: docs-consistency + manual grep</li><li>[ ] AC for 1.4 — npm-hello expects 1.1.0; CI path documented — test: <code>examples/npm-hello/smoke.mjs</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>npm registry does not yet have 1.1.0 when CI runs — soft-skip registry path, pack local tarball, still smoke exports.</li><li>Historical <code>_audit/archive/</code> mentions of 1.0.0 must not be rewritten.</li><li>Native store scaffold \"until LAUNCH\" release-notes placeholders become post-LAUNCH scaffold copy (no product store submit).</li></ul>\n<h2>4. Protected invariants this task must not weaken</h2>\n<ul><li>No <code>CHANGELOG.md</code> surface.</li><li>License stays <strong>UNLICENSED</strong>; consumer grant unchanged except version references.</li><li>Expansion Rule and axis contracts unchanged.</li><li>Do not invent product → element mappings.</li></ul>";
