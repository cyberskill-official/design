window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-008"]="<h1>TASK-IMP-008: Re-gate token auto-commit + resolve .mcp.json</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-021 and FIND-024.</p>\n<ul><li>1.1 Prefer: remove <code>[skip ci]</code> from <code>regenerate-tokens-push</code> <strong>or</strong> drop auto-push and rely on PR fail-closed regen (assessment preference: PR-required freshness). Document the chosen policy in <code>docs/ci-cd.md</code> / decisions if needed.</li><li>1.2 Confirm branch protection out-of-band (NV-18.1) — document observed state; do not invent GitHub settings.</li><li>1.3 Resolve tracked <code>.mcp.json</code> dangling <code>.cyberos/</code> MCP ref via gitignore-safe local override or a tracked stub that does not point at a missing vendored path.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — tip either has no <code>[skip ci]</code> on regen push, or auto-push removed with PR freshness path documented — workflow + docs</li><li>[x] AC for 1.2 — NV-18.1 note recorded (observed / unknown) — task notes or ci-cd.md</li><li>[x] AC for 1.3 — <code>.mcp.json</code> no longer references a broken required path for fresh clones — inspect + optional soft-skip docs</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Local operators may still need a private MCP override — prefer untracked / example file over a broken tracked command.</li><li>Do not disable regenerate-tokens PR fail-closed check while changing the push job.</li></ul>\n<h2>4. Protected invariants this task must not weaken</h2>\n<ul><li>No Mentions / Sortable / publish classifier changes here (owned by 002/003/005).</li><li>Never push/merge without operator instruction.</li></ul>\n<h2>5. Implementation notes (Aug 2026)</h2>\n<ul><li><strong>FIND-021 policy chosen:</strong> safer minimal change — remove <code>[skip ci]</code> from <code>regenerate-tokens-push</code> so auto-pushed tips re-enter the gate board. PR twin stays fail-closed (contributor freshness path). Documented in <code>docs/ci-cd.md</code> (+ VI) and <code>docs/decisions.md</code> §5.</li><li><strong>NV-18.1:</strong> recorded as <strong>unknown / confirm out-of-band</strong> under <code>docs/ci-cd.md</code> Branch protection — no invented GitHub Settings values.</li><li><strong>FIND-024:</strong> tracked <code>.mcp.json</code> → <code>scripts/mcp/cyberos-mcp.mjs</code> stub; forwards when <code>.cyberos/</code> present after install; clean exit on fresh clone. Shape mirrored in <code>.mcp.json.example</code>; CONTRIBUTING documents the contract.</li></ul>\n<p><em>End of TASK-IMP-008.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"i accept all, continue\"</strong> — Phases 0–2 tranche (TASK-IMP-001…008) accepted. Status set to <code>done</code>.</p>";
