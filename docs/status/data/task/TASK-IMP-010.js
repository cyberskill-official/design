window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-010"]="<h1>TASK-IMP-010: Rating/Tree/Toolbar APG keyboard + a11y fixtures</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-014: Rating, Tree, and Toolbar declare ARIA interaction roles without the APG keyboard model.</p>\n<ul><li>1.1 <strong>Rating</strong> — radiogroup roving tabindex + ArrowLeft/Right (and Up/Down) selection; single tab stop when interactive.</li><li>1.2 <strong>Tree</strong> — tree navigation (ArrowUp/Down, Left/Right expand-collapse, Home/End) + roving tabindex on visible treeitems.</li><li>1.3 <strong>Toolbar</strong> — arrow navigation among toolbar controls; resolve role mix (menubutton + popup <code>menu</code> for overflow is OK; do not nest <code>menu</code> as a toolbar peer without keyboard). Escape closes overflow.</li><li>1.4 Extend <code>_audit/a11y-gate.html</code> with hard-fail keyboard fixtures for all three; update lede/<code>_audit/index.html</code> copy that currently says Phase-3 not hard-failed.</li><li>1.5 Propagate Expansion Rule surfaces (<code>.d.ts</code> / <code>.prompt.md</code> notes, Atomic View if needed, behavior harness where interactive contracts change).</li><li>1.6 This task MUST NOT be marked <code>done</code> without HITL.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1–1.3 — each widget matches its APG keyboard model — demo + a11y-gate hard-fail</li><li>[x] AC for 1.4 — fixtures fail if arrow/roving regress — <code>_audit/a11y-gate.html</code></li><li>[x] AC for 1.5 — Expansion Rule checklist for touched components</li><li>[x] AC for 1.6 — human review + final acceptance — HITL only (operator 2026-08-09)</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Rating <code>readOnly</code>: no tab stop / no arrow mutation.</li><li>Tree empty / all-leaf: arrows still move among items; Left/Right no-op when no children.</li><li>Toolbar with only separators or zero items: no crash.</li></ul>\n<h2>4. Protected invariants this task must not weaken</h2>\n<ul><li>Existing Rating fill/<code>aria-checked</code> contract used by a11y semantics test unless intentionally updated with fixture.</li><li>Mentions/Sortable keyboard fixtures remain green.</li></ul>\n<p><em>End of TASK-IMP-010.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"continue all of them\"</strong> — Phase 3 tranche (TASK-IMP-009…012) accepted. Status set to <code>done</code>.</p>";
