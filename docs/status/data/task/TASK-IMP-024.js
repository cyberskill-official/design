window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-024"]="<h1>TASK-IMP-024: HTML-faithful PDF/DOCX download + drop counsel banner</h1>\n<h2>1. Description (normative)</h2>\n<p>Operator preview of the framework agreement still shows a counsel-review warning banner, and downloaded PDF/DOCX attachments do not match the on-screen HTML. Two separate export buttons are the wrong control: one Download control with a PDF | DOCX menu.</p>\n<ul><li>1.1 Remove the on-sheet counsel-review banner from <code>templates/vn-framework-agreement</code> (the quoted bilingual warning). Keep catalog ⚠ / docs counsel notes that are not that banner. Update the HR fixture so source-parity stays honest.</li><li>1.2 Downloaded PDF and DOCX must be generated from the live <code>.cs-sheet</code> so they match the HTML preview: same visible text, tables, bilingual runs, lockup. PDF prefers a sheet-clone print / raster of the rendered sheet (not Storybook <code>location.href</code>); DOCX walks the same sheet without duplicating the lockup into the Word header and without smashing adjacent runs.</li><li>1.3 Replace the two toolbar buttons with one <strong>Tải / Download</strong> control that opens a menu of <strong>PDF</strong> and <strong>DOCX</strong>. Print chrome stays hidden. No CDN. CSP unchanged.</li><li>1.4 Extend <code>test-doc-export</code> for the single download menu + framework banner absence + DOCX↔sheet text overlap; keep no-eval / source-parity green.</li><li>1.5 Leave HITL for final acceptance — land at <code>ready_to_review</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — framework HTML (and its DOCX export) do not contain the quoted counsel banner</li><li>[x] AC for 1.2 — PDF/DOCX built from <code>.cs-sheet</code>; DOCX keeps tables + VN diacritics; PDF still <code>%PDF</code> + A4/Letter</li><li>[x] AC for 1.3 — one Download trigger + PDF/DOCX menu; old dual buttons gone</li><li>[x] AC for 1.4 — <code>test-doc-export</code> + <code>test-docx-source-parity</code> green</li><li>[ ] AC for 1.5 — human review + final acceptance — HITL only</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Raster PDF may fail on Safari foreignObject — fall back to printing a same-origin sheet clone (not the Storybook URL).</li><li>iOS iframe <code>a.download</code> is weak — prefer <code>navigator.share({ files })</code> then blob download.</li><li>Word still reflows page breaks; PDF is the pagination-faithful artifact. DOCX must not lose sheet text.</li><li>Do not reintroduce <code>unsafe-eval</code>. Do not commit Word/PDF binaries.</li><li>Do not remove counsel notes on other instruments (contractor, compensation) unless they are the quoted banner.</li></ul>\n<h2>4. Protected invariants</h2>\n<ul><li>Site CSP unchanged (<code>script-src 'self' 'unsafe-inline'</code>).</li><li><code>templates/*/support.js</code> remain byte-identical.</li><li>Decks stay PPTX.</li></ul>\n<h2>5. Implementation notes (Aug 2026)</h2>\n<ul><li>Banner lived only on <code>VnFrameworkAgreement.dc.html</code> (plus the fixture phrase).</li><li>Prior PDF path opened <code>location.href</code> (Storybook iframe / blank capture) and print CSS zeroed sheet padding, so the attachment did not look like the HTML card.</li><li>PDF download now paints the live <code>.cs-sheet</code> to canvas (boxes + text + inline SVG) as <strong>one JPEG page</strong> whose MediaBox matches the card aspect. A4 slicing cut tables mid-row and stretched the leftover slice (squashed last page). SVG <code>foreignObject</code> taints canvas and is not used. Print of a same-origin sheet clone remains the fallback.</li><li>DOCX: Word header/footer stay empty (sheet already has lockup + footer); yellow <code>--cs-doc-fill</code> blanks become highlight; <code>colspan</code> becomes <code>w:gridSpan</code>.</li><li>Prior DOCX dumped the first child into the Word header <em>and</em> the body, flattened nested cell labels, and dropped whitespace-only text nodes.</li></ul>\n<p><em>End of TASK-IMP-024.</em></p>";
