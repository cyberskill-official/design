window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-029"]="<h1>TASK-IMP-029: Editor schema-based rich text</h1>\n<h2>1. Description (normative)</h2>\n<p>Containment landed 2026-08-24: <code>Editor</code> sanitizes <code>defaultValue</code>; trusted seed is opt-in via <code>unsafeHtml</code> (documented trust boundary). This wave adds a <strong>schema allowlist</strong> for the HTML value model. Full TipTap/ProseMirror-class replacement of contentEditable remains a future product decision (not in this task’s delivered scope).</p>\n<ul><li>1.1 Constrain the HTML value model with an allowlist of nodes/marks (<code>EDITOR_SCHEMA</code>).</li><li>1.2 Keep <code>unsafeHtml</code> / sanitize API stable (plus exported <code>sanitizeHtml</code> / <code>EDITOR_SCHEMA</code>).</li><li>1.3 Unit + docs; leave HITL for final acceptance.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — schema allowlist ships; no raw HTML injection by default (<code>editor-schema.js</code> + sanitize on seed and emit)</li><li>[x] AC for 1.2 — API / docs migration notes EN·VI (prompt + d.ts + Conventions lifecycle note)</li><li>[x] AC for 1.3 — human review + final acceptance — HITL only (operator 2026-08-24)</li></ul>\n<h2>3. Implementation notes (2026-08-24)</h2>\n<ul><li><code>components/forms/editor-schema.js</code> — <code>EDITOR_SCHEMA</code> + <code>sanitizeHtml</code> (DOM + Node paths).</li><li>Emitted <code>onChange</code> HTML is re-sanitized.</li><li>Gate: <code>_audit/ci/test-editor-sanitize.mjs</code>.</li><li>Follow-up (new task if prioritized): replace contentEditable/<code>execCommand</code> with a schema editor library.</li></ul>\n<p><em>End of TASK-IMP-029.</em></p>\n<h2>Human acceptance (2026-08-24)</h2>\n<p>Operator instruction: <strong>approved TASK-IMP-025/028/029</strong> (HITL accept). Status set to <code>done</code>.</p>";
