window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-011"]="<h1>TASK-IMP-011: Auth pattern error/loading/invalid variants</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FLOW-013 / NV-9.2: the copyable auth pattern is happy-path-only.</p>\n<ul><li>1.1 <code>templates/auth/Auth.dc.html</code> SHALL expose a <code>formState</code> (or equivalent) tweak: <code>default</code> | <code>loading</code> | <code>error</code> | <code>invalid</code> with production-honest UI (loading CTA, error alert, invalid field messaging) EN·VI.</li><li>1.2 <code>ui_kits/status-hub/login.html</code> SHALL demonstrate the same state variants (tweak control or clearly labeled demo states) so kit consumers can copy non-happy paths.</li><li>1.3 Related guideline/kit card copy updated if it claims happy-path-only completeness.</li><li>1.4 This task MUST NOT be marked <code>done</code> without HITL.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — Auth template enum switches all four states — inspect + Storybook/template render</li><li>[x] AC for 1.2 — Status Hub login shows error/loading/invalid — inspect <code>login.html</code></li><li>[x] AC for 1.3 — no stale \"happy path only\" claim left in touched docs</li><li>[x] AC for 1.4 — human review + final acceptance — HITL only (operator 2026-08-09)</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Remember-me stays default-off (FIND-011).</li><li>Invalid state uses field <code>aria-invalid</code> / error text; error state uses a status/alert live region.</li></ul>\n<h2>4. Protected invariants this task must not weaken</h2>\n<ul><li>No seed password values; bilingual EN·VI for template strings.</li></ul>\n<p><em>End of TASK-IMP-011.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"continue all of them\"</strong> — Phase 3 tranche (TASK-IMP-009…012) accepted. Status set to <code>done</code>.</p>";
