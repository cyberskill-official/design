window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-023"]="<h1>TASK-IMP-023: Blank print preview on iOS/Safari</h1><h2>1. Description (normative)</h2><p>After TASK-IMP-022 export landing, operator print preview (iOS Options \u2192 PDF) shows a blank white page (Page 1 of 1) while on-screen content is present. Chromium <code>page.pdf</code> CI still passes \u2014 this is a print-layout / iframe capture bug, not missing content.</p><ul><li>1.1 Strengthen <code>@media print</code> so <code>html</code> / <code>body</code> / <code>#dc-root</code> / <code>.sc-host</code> use <code>height: auto !important</code> and <code>overflow: visible !important</code> (FULL_PAGE_CSS <code>height:100%</code> must not clip the sheet in Safari print).</li><li>1.2 PDF toolbar action: when running inside an iframe (Storybook), open the same document URL top-level and print from there so iOS Safari does not capture an empty iframe viewport; fall back to <code>window.print()</code>.</li><li>1.3 Extend <code>test-doc-export</code> to assert print-media computed heights are unconstrained; keep support-runtime identity + no-eval gates green.</li><li>1.4 Leave HITL for final acceptance \u2014 land at <code>ready_to_review</code>.</li></ul><h2>2. Acceptance criteria</h2><ul><li>[x] AC for 1.1 \u2014 print CSS unlocks height/overflow on root chain \u2014 vendor support.js + sync (85 copies)</li><li>[x] AC for 1.2 \u2014 PDF button uses top-level print when framed \u2014 <code>printPdf()</code> in doc-export.js</li><li>[x] AC for 1.3 \u2014 <code>test-doc-export</code> asserts print height auto; 62/62 green \u2014 CI</li><li>[ ] AC for 1.4 \u2014 human review + final acceptance \u2014 HITL only</li></ul><h2>3. Edge cases</h2><ul><li>Popup blockers: fall back to in-frame <code>window.print()</code> if <code>window.open</code> fails.</li><li>Do not reintroduce <code>unsafe-eval</code>.</li><li>Decks stay PPTX; this task only touches print-doc path / shared DC print baseline.</li></ul><h2>4. Protected invariants</h2><ul><li><code>templates/*/support.js</code> remain byte-identical.</li><li>Site CSP unchanged.</li></ul><h2>5. Implementation notes (Aug 2026)</h2><ul><li>Root cause: <code>FULL_PAGE_CSS</code> (<code>html,body,#dc-root,.sc-host{height:100%}</code>) left the print root at viewport height; Safari/iOS print clipped to that box (often blank), especially from Storybook iframes.</li><li>Fix: print baseline unlocks the root chain; export chrome duplicates the rule; PDF click prefers <code>window.open(location.href)</code> + print when framed.</li></ul><p>*End of TASK-IMP-023.*</p>";
