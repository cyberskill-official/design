window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-009"]="<h1>TASK-IMP-009: RSC <code>\"use client\"</code> / client barrel</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-006: default bundler entry ships raw JSX with zero <code>\"use client\"</code> boundaries, so Next.js App Router Server Components cannot import interactive components.</p>\n<ul><li>1.1 <code>generate-react-entry.mjs</code> SHALL emit a leading <code>\"use client\";</code> directive on <code>_esm/react.mjs</code> (client barrel for the default package entry).</li><li>1.2 <code>test:react-entry</code> (and/or package-exports integrity) SHALL assert the directive is present on the generated barrel.</li><li>1.3 <code>docs/consuming.md</code> (and VI if mirrored) Next/SSR block SHALL document that the default entry is a client boundary; Server Components may import from <code>@cyberskill/design</code> without per-import shims.</li><li>1.4 This task MUST NOT be marked <code>done</code> without HITL.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — <code>_esm/react.mjs</code> starts with <code>\"use client\"</code> after regen — inspect + <code>generate-react-entry --check</code></li><li>[x] AC for 1.2 — unit gate fails if the directive is removed — <code>npm run test:react-entry</code></li><li>[x] AC for 1.3 — consuming docs mention client barrel / RSC — inspect <code>docs/consuming.md</code></li><li>[x] AC for 1.4 — human review + final acceptance — HITL only (operator 2026-08-09)</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Legacy <code>@cyberskill/design/legacy</code> (<code>_esm/cs.mjs</code>) remains browser-only; do not add <code>\"use client\"</code> there.</li><li>Pure-presentational modules may stay without per-file directives when the barrel carries the boundary.</li></ul>\n<h2>4. Protected invariants this task must not weaken</h2>\n<ul><li>Export parity with <code>_ds_bundle.js</code> header; peer React model; no CDN in react.mjs.</li></ul>\n<p><em>End of TASK-IMP-009.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"continue all of them\"</strong> — Phase 3 tranche (TASK-IMP-009…012) accepted. Status set to <code>done</code>.</p>";
