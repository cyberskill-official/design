window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-018"]="<h1>TASK-IMP-018: SBOM + OpenSSF Scorecard</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-023.</p>\n<ul><li>1.1 Add a release-path CycloneDX SBOM generation step (workflow and/or <code>npm</code> script) that produces an SBOM artifact for each publish/tag.</li><li>1.2 Add an OpenSSF Scorecard GitHub Actions workflow (official action) with SARIF/artifact upload as appropriate.</li><li>1.3 Document where operators find the SBOM/Scorecard outputs (<code>docs/ci-cd.md</code> / release runbook).</li><li>1.4 Leave HITL for final acceptance — land at <code>ready_to_review</code>.</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — SBOM generated on release path — workflow/script</li><li>[x] AC for 1.2 — Scorecard workflow present — <code>.github/workflows</code></li><li>[x] AC for 1.3 — docs point at artifacts — ci-cd / release runbook</li><li>[x] AC for 1.4 — human review + final acceptance — HITL only (operator 2026-08-09)</li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Scorecard may need <code>permissions</code> + optional PAT for private repos; keep public-repo defaults.</li><li>Do not commit giant generated SBOMs to tip unless the chosen policy says so — artifact upload is fine.</li></ul>\n<h2>4. Protected invariants</h2>\n<ul><li>OIDC publish path unchanged (no token reintroduction).</li></ul>\n<h2>5. Implementation notes (Aug 2026)</h2>\n<p>Workflows <code>sbom.yml</code> + <code>scorecard.yml</code>; <code>npm run sbom</code>; documented in release-runbook + ci-cd.</p>\n<p><em>End of TASK-IMP-018.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"accept all, continue\"</strong> — Phase 4–5 tranche (TASK-IMP-013…020) accepted. Status set to <code>done</code>.</p>";
