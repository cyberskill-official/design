window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-IMP-005"]="<h1>TASK-IMP-005: npm-publish fail on 403/EOTP + registry presence</h1>\n<h2>1. Description (normative)</h2>\n<p>Close FIND-020: real publish denials currently soft-skip (<code>process.exit(0)</code>).</p>\n<ul><li>1.1 Split soft-skip: keep exit 0 for expected no-ops (<code>missing_secrets</code>, already-published / fork cases).</li><li>1.2 <strong>Fail</strong> (non-zero) on 403 and EOTP (and similar hard auth/permission denials when a publish was attempted).</li><li>1.3 After a claimed successful publish, verify registry presence via <code>npm view @cyberskill/design@VERSION</code>.</li><li>1.4 Add unit/fixture coverage for the classifier if a publish-runner test pattern exists (extend <code>_audit/ci/test-code-connect.mjs</code>-style asserts or a dedicated test).</li></ul>\n<h2>2. Acceptance criteria</h2>\n<ul><li>[x] AC for 1.1 — missing_secrets / already-published still soft-skip exit 0 — unit cases</li><li>[x] AC for 1.2 — 403 and EOTP fail closed — unit cases</li><li>[x] AC for 1.3 — successful path runs post-publish <code>npm view</code> check — code + dry-run docs</li><li>[x] AC for 1.4 — classifier tests land in CI-visible path — <code>_audit/ci/</code></li></ul>\n<h2>3. Edge cases</h2>\n<ul><li>Local non-OIDC without secrets remains soft-skip (not a silent failed publish).</li><li>EPUBLISHCONFLICT / already-published: keep soft-skip (honest report + exit 0); 403/EOTP are hard-fail.</li></ul>\n<h2>4. Protected invariants this task must not weaken</h2>\n<ul><li>Trusted Publishing OIDC path remains the only CI auth mode; do not reintroduce classic long-lived tokens.</li><li>Update <code>docs/decisions.md</code> §7 soft-skip wording if it still claims soft-skip on 403/EOTP after this change.</li></ul>\n<p><em>End of TASK-IMP-005.</em></p>\n<h2>Human acceptance (2026-08-09)</h2>\n<p>Operator HITL: <strong>\"i accept all, continue\"</strong> — Phases 0–2 tranche (TASK-IMP-001…008) accepted. Status set to <code>done</code>.</p>";
