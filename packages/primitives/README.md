# @cyberskill/primitives

Compiled focus trap, overlay stack, sibling `inert`, sanitizer, collection helpers (`nextRovingIndex`, `wrapIndex`, `reduceListbox`), and opt-in `reportAdoption` telemetry. No templates. Rating, Carousel, and Combobox consume those helpers from `components/_utils/roving.js` (same source this package compiles).
