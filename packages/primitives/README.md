# @cyberskill/primitives

Compiled focus trap, overlay stack, sanitizer, collection/roving (`nextRovingIndex`), and opt-in `reportAdoption` telemetry. No templates. React hooks remain here until a DOM-only split is demanded. Rating consumes `nextRovingIndex` from `components/_utils/roving.js` (same source this package compiles).
